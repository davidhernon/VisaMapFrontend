export interface CountryDetails {
    code: 'AF'
    details: {
      covidBan: boolean
      eVisa: boolean
      length: null | number
      visaOnArrival: boolean
      visaRequired: boolean
    }
  }